# 手机控制（无跳跃键）

- 左半屏按下：手指位置出现圆形摇杆，推着走
- 右半屏滑动：转视角
- 没有「跳」键；要跳用游戏自带右侧那个键
- 松开左手摇杆消失

把 `joystick-eagler.js` 放到 mc.cq.je 站点目录，`index.html` 加：
```html
<script src="/joystick-eagler.js"></script>
```

游戏自带的左边十字键会和摇杆重叠。进游戏后点右上角暂停 → 选项 → 控制，能关触屏方向键就关掉。
或按 F1 隐藏 HUD（热键栏也会隐）。
